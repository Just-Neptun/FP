% !TeX root = ../main.tex

\section{Solving Sudokus With Brainscales-2}

We will apply the simulated neurons to solve a given Sudoku problem. For an \( n \times n \) Sudoku grid, there are \( n^2 \) fields which could each be \( n \) different numbers. We will use \( n \) neurons for each field. These will each represent one of the possible output numbers, using a \quotedef{one hot encoding}.

We will use the Brainscales-2 system within Python to set up the neurons and synapses in order to solve the Sudoku. The created and used code is included as attached \texttt{.ipynb} and \texttt{.py} files.

The rules of Sudoku are encoded as constraints of the program. This means inhibitory synapses are set up so that spiking neurons inhibit activity in other neurons which would cause a disallowed board state.

These rules include:
\begin{itemize}
    \item Only one number per field
    \item Only one of each number per row
    \item Only one of each number per column
    \item Only one of each number within smaller grid boxes which cover the whole Sudoku
\end{itemize}

The implementation of these rules by connecting neurons is explained further in \autoref{sec:sudoku_counting_neurons}.

\subsection{Running Network with No Constraints}

As a \quoteapprox{control}, we will run the program on a \( 4 \times 4 \) Sudoku without implementing any constraint rules. The resulting \quoteapprox{solution} is shown in \autoref{fig:sudoku_no_constraints}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{./04 Sudoku/no_constraints_sudoku.png}
    \caption{Sudoku Solver Without Constraint Synapses}
    \label{fig:sudoku_no_constraints}
\end{figure}

Clearly, without implementing the actual rules/constraints of Sudoku, the solver is incomplete. The resulting grid contains multiple of the same number in the same row, column and box!

In the graph of the neuron activity on the right, we can see the initial clues given within the first \( \approx \qty{0.02}{\milli \second} \). These neurons are continually stimulated by the clue-giving neurons, so we see their activation is consistent and high throughout the solve.

Due to the Poisson noise stimulating every neuron, they all spike randomly during the whole process as well. There are no inhibitory synapses, so they do not influence each other or \quoteapprox{settle down} into a stable solution state.

We also note that the desired output (one-hot encoding) is not followed. Multiple neurons representing different numbers (different colors on the graph) in the same field (within the same thick horizontal lines) are firing at the same time.

\subsection{Running Network With Constraints}

After implementing the rules of Sudoku as inhibitory synapses in the network, we rerun the program, obtaining the solution seen in \autoref{fig:sudoku_with_constraints}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{./04 Sudoku/with_constraints_sudoku.png}
    \caption{Sudoku Solver With Constraint Synapses}
    \label{fig:sudoku_with_constraints}
\end{figure}

This time, the Sudoku is actually solved (as evident by inspection as well as the green border).

Again we can see the clues as constant spiking neurons in the activity graph. These inhibit other numbers from being activated within their fields. The \quoteapprox{deduced} numbers can be seen after at least \qty{0.04}{\milli \second}. These are less consistent spikes, but they even out towards the end as the simulation approaches the solution.

We also note the small number of \quoteapprox{misfires}, were a neuron representing a different number fires, even though a different number is correct. This sometimes happens even if that number was already deduced, or is given as the clue. This is caused by the Poisson noise, which also allows the system to work at all, so it is necessary and expected. Luckily, these neurons do not continuously activate. They are quickly inhibited by the other neurons again, so their effects do not cause the solution to fail.

\subsection{Varying the Number of Clues}

We examine how the program performs when given different numbers of clues.

As it was successful with 8 clues, it is no surprise that it also solves the Sudoku when given more clues. This makes sense, as more clues mean fewer fields need to be solved for. More clues also constrain the solution more, preventing the network from \quoteapprox{guessing} and cementing numbers which make the board impossible to solve further.

Interestingly, this guessing behavior has both advantages and disadvantages. It is most apparent when \emph{no} clues are given. In this case, the solution is underdetermined. There is not enough information to logically solve the Sudoku. Some assumptions or guesses need to be made in order to continue. The network achieves this well, leading to valid solution boards for zero clues. This also applies when 1 or 2 clues are given.

However, the guessing becomes a problem for an intermediate number of clues, between 4 and 5. In this range, the system only sometimes solves the problem correctly. The solution might still be underdetermined, but it is constrained much more than for only 1 or 2 clues.

In many cases, the network seems to have made a guess which fits within what is allowed by the clues but is not logically justified. After this point, logically consistent arguments can be made to solve more fields until the problem is almost finished. However, the model runs into a problem at this point because the clues constrain the final fields too much.

An example of this is seen in \autoref{fig:sudoku_underdetermined}. Ignoring the duplicate 4 in the lower right, we see that the rest of the Sudoku is solved correctly. The network seems to have run into a \quotedef{dead-end} during the solve. All the numbers which fit together correctly inhibit other neurons from activating. Because the Sudoku is in large part solved, the neurons can no longer be inhibited and stopped from spiking, leading to an invalid end state.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{./04 Sudoku/underdetermined_sudoku.png}
    \caption{A Failed Attempt at Solving the Sudoku With 5 Clues}
    \label{fig:sudoku_underdetermined}
\end{figure}

To reduce these errors, the Poisson noise or its weight could be adjusted. This makes guesses less likely overall. The weights of the other synapses could also be tweaked to see what makes the success rate highest.

Excitatory synapses could also be used to encourage the numbers other than the current number in other fields in a row/column/block. This would reduce random guesses while still allowing logical steps.

Lastly, the program could be set to repeat if the solution is not found the first time. This could also be done while saving some information about the failed board state to avoid those mistakes in the future. From the example in \autoref{fig:sudoku_underdetermined}, the duplicate 4 could be saved and the solver run two more times with the 4 in each spot as an additional clue.

These methods might increase the success rate by preventing dead-end solution attempts and to encourage more logical reasoning.

\subsection{Investigating Success Rate and Number of Clues}



\subsection{Unnecessary Constraint}


\subsection{Implementation and Counting Required Neurons and Synapses}
\label{sec:sudoku_counting_neurons}

We set up neurons to represent the board state. We also use additional neurons to add random noise to the calculation (this avoids dead-ends where a mistake in the solve happened). These are connected by synapses.

In order to sustain activity and \quoteapprox{save} a value when a number is determined, neurons are linked to themselves.

The constraints/rules of the Sudoku are encoded in other synapses between different groups of neurons.

These will be explained for a \( 4 \times 4 \) and a \( 9 \times 9 \) Sudoku grid. The exact number of neurons and synapses needed will be calculated as well.

\subimport{./04 Sudoku/}{4x4_summary.tex}

\subimport{./04 Sudoku/}{9x9_summary.tex}

\begin{itemize}
    \item Task 1: Run the network without any constrains, observe the spike pattern and the solution of the sudoku. Try to
    explain.
    \item Task 2: Now implement the four constraint rules discussed above. If they are correctly implemented you should
    get a solved sudoku (keep number of hints and seed).
    Now you have a working sudoku solver. Let’s test it:
    \item Task 3: Vary the number of clues. If the solver fails can you find an explanation for that behavior? Can you think
    of possible strategies to reduce such errors? (It might help to inspect the code and numpy documentation)
    \item Task 4: What do you expect to happen, if you set the number of clues to zero? Check your hypothesis. Can you
    explain your observation?
    \item Task 5: Now, investigate how the success rate is related to the number of clues. For this, vary the number of clues
    from four to ten. Repeat each configuration ten times, while keeping the sudoku fixed. Visualize your result.
    \item Task 6: Is there a constraint that is not necessarily required? Why?
    \item Task 7: Crunching some numbers: How many neurons and how many synaptic connections were required in this
    task? How many would be required for a 9x9 sudoku?
\end{itemize}
